{
  "name": "svg-animation-compatibility-gallery",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "scripts": {
    "generate": "bun run src/main.ts",
    "regen": "bun run src/main.ts",
    "dev": "bun run src/main.ts",
    "clean": "bun run src/clean.ts",
    "check": "tsc --noEmit",
    "count": "bun run src/main.ts --count"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
